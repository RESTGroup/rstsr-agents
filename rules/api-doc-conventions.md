# API Doc Conventions (rstsr-core) — router

The normative rstsr-core API docstring policy lives in the skill
**`api-doc-conventions`** (`skills/api-doc-conventions/SKILL.md` of this
repository), which agent sessions load automatically when a documentation task
matches. This file only routes you there — policy content is never duplicated
here.

Quick orientation (details in the skill):

- Terminology (anchor function, variant, alias, doc_draft twin, ...):
  `CONTEXT.md` at this repository's root.
- Campaign plan (wholesale retrofit, batches 0–6): `plans/2026-09-api-doc-campaign.md`
  (local-only, gitignored — if absent in this checkout, ask the maintainer).
- Tracking: `rstsr-core/tests/tracking/doc_coverage.csv`.
- Doctest verification: `cargo test -p rstsr-core --doc` (CI-gated).

<!-- Router created 2026-09-06; policy content generated by AI (model glm-5.3[1m],
     Claude Code session) and maintained in the skill. -->
